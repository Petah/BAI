application.title=BAI
application.vendor=Petah
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=bai/0.1/SkirmishAI.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.AIInterface-src.jar=lib/java-interface-0.1/jlib/AIInterface-src.jar
file.reference.AIInterface.jar=lib/java-interface-0.1/AIInterface.jar
file.reference.org.petah.common.jar=lib/org.petah.common.jar
file.reference.jdom.jar=lib/jdom.jar
file.reference.jna.jar=lib/java-interface-0.1/jlib/jna.jar
file.reference.vecmath.jar=lib/java-interface-0.1/jlib/vecmath.jar
includes=**
jar.compress=true
javac.classpath=\
    ${file.reference.org.petah.common.jar}:\
    ${file.reference.jdom.jar}:\
    ${file.reference.AIInterface.jar}:\
    ${file.reference.AIInterface-src.jar}:\
    ${file.reference.jna.jar}:\
    ${file.reference.vecmath.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=true
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jaxbwiz.endorsed.dirs="${netbeans.home}/../ide12/modules/ext/jaxb/api"
jnlp.codebase.type=local
jnlp.codebase.url=file:/G:/Programming/Java/BAI/dist/
jnlp.descriptor=application
jnlp.enabled=false
jnlp.offline-allowed=false
jnlp.signed=false
main.class=org.petah.spring.bai.TestMain
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xss20m  -Xmx512m
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
